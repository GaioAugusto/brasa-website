import { CommonStrings } from "./CommonStrings";
import { TemplateStrings } from "./TemplateStrings";

export interface ModulesIdentifier {
  readonly common: CommonStrings;
  readonly templates: TemplateStrings;
}
