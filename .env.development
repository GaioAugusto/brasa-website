# Loaded by `npm run dev` (Vite mode=development).
# local + local: talk to the backend running on your machine (brasa-website-api:
# `docker compose up -d` + `npm run start:dev`). No deploy needed.
VITE_USERS_API_BASE_URL=http://localhost:8080
VITE_EVENTS_API_BASE_URL=http://localhost:8080
